mixin loginForm()
    form.form(action="/login" method="POST")
        h2 Login
        label(for="email") Email Adress
        input#mail(type="email", name="email")
        label(for="password") Password
        input#pass(type="password", name="password")
        input.button(type="submit", value="Login in")